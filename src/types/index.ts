export enum EPage {
  HOME = 'home',
  PROJECTS = 'projects',
  CONTACT = 'contact',
  NOT_FOUND = 'not_found',
}

export enum ENavigationOptions {
  ABOUT = 'about',
  CONTACT = 'contact',
  HOME = '/',
  PROJECTS = 'projects',
}
